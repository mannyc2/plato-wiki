# Bun loads .env files automatically. Local configuration is optional.
# External audio artifact directory, used only when building a site with recordings.
# PLATO_RECORDING_ARTIFACT_ROOT=/absolute/path/to/audio-artifacts

# Commentary campaign authentication belongs to the Codex CLI session.
# No model-provider key is needed for curation tools, tests, validation, or builds.
