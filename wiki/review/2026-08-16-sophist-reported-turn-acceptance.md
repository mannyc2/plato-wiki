# Sophist reported-turn cohort acceptance

- date: 2026-08-16
- scope: `turn_sophist_0592` (244b), `turn_sophist_0956` (258d)
- source: `raw/plato/greek/sophist.txt`, SHA-256
  `4e856d5f7b7c4d870fd0fbce7c61060fd9c85e6c708ba39d6f18b329cb4ebbeb`

## Decision

Accept the complete four-record cohort after direct Greek review and an
independent Greek-only second read.

- At 244b, `ΞΕ.` remains the frame. The first `{q}` is his own staged question;
  the nested `{q} φαμὲν γάρ, {/q}` is retained unresolved because `φήσουσιν`
  attributes it only to the plural ἓν τὸ πᾶν λέγοντες.
- At 258d, `ΞΕ.` remains the frame and the bounded `{quote}` verse is retained
  unresolved. The antecedent `Παρμενίδῃ` and `ὁ μέν πού φησιν` identify its
  source owner, but the Sophist registry has no terminal owner for that verse.

Only `ΞΕ.` was added to the Sophist sigla registry to license the printed
frames. No cutover or voice join is authorized by this acceptance.
